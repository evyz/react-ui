import * as React from 'react'
import { GridOptions } from "./src/calendar.types"

const Grid = (props: GridOptions) => {
    return (
        <table>
            <thead>

            </thead>
            <tbody>

            </tbody>
        </table>
    )
}

export default Grid